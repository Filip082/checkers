services:
  server:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    command: npm run
    depends_on:
      - db
    environment:
      - NODE_ENV=production
  db:
    image: postgres
    restart: always
    env_file:
      - .env
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql
      - ./database:/docker-entrypoint-initdb.d:z
volumes:
  postgres-data:
